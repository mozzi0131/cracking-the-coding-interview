// 주어진 이진 트리에서 깊이별로 연결 리스트를 만들어 내는 알고리즘을 작성하라.

#include <vector>
#include <list>

#include "mytree.h"

std::vector<std::list<int>> createList(BinaryTree *tree){
  std::vector<std::list<int>> ans;

  return ans; 
}
